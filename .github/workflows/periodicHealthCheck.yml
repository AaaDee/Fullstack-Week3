
on:
  schedule:
    - cron: '30 * * * *'

jobs:
  periodic_health_check:
    runs-on: ubuntu-20.04
    steps:
    - name: Check the deployed service URL
      uses: jtalk/url-health-check-action@v2
      with:
        url: https://winter-sky-6704.fly.dev/
     