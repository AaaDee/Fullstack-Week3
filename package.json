{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && cd phonebook-front && npm run build --prod && cp -r build ..",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cypress": "^10.11.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.12",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "eslint": "^8.25.0",
    "eslint-config-react-app": "^7.0.1",
    "eslint-plugin-cypress": "^2.12.1",
    "eslint-plugin-react": "^7.31.10",
    "nodemon": "^2.0.7"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
